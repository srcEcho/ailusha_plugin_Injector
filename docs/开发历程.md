# QuestLog 插件注入 — 所有尝试路线总结

> 2026-07 ~ 2026-08-05 | 目标：在 EnigmaVB 打包的 Game.exe 中加载外部 QuestLog 插件
> 最终状态：**✅ 第36版找到可行方案 — 浏览器进程 ReadFile+CreateFileW 双重Hook + 磁盘重定向**

---

## 基础设施（已验证稳定）

| 组件 | 状态 | 备注 |
|------|------|------|
| version.dll 代理 | ✅ | 32-bit, -Os, LoadLibraryExA防递归 |
| DLL注入渲染进程 | ✅ | CreateRemoteThread + LoadLibraryW |
| QPC Hook (kernel32) | ✅ | 唯一100%稳定的Hook点 (368K调用/30s) |
| nw_elf.dll 导出表 | ✅ | 10,480函数,完整C++符号,84.8MB |
| nw_elf.dll base | 0x10000000 | ASLR在此版本固定 |
| **浏览器进程 ReadFile Hook** | ✅ | **关键突破 — JS文件在浏览器进程加载！** |

## 完整测试矩阵（36条路线）

### 第一阶段：文件系统 Hook (渲染进程)

| # | 方案 | 结果 | 死因 |
|---|------|------|------|
| 1 | CreateFileW 重定向 | ⚠️ | 自重定向OK，跨路径挂起 |
| 2 | ReadFile 内容替换 | ❌ | Chromium不走kernel32 ReadFile读JS |
| 3 | MapViewOfFile | ❌ | 纯直通崩溃 |
| 4 | GetFileSize | ✅ | 稳定但无用 |

### 第二阶段：CDP / 命令行

| # | 方案 | 结果 | 死因 |
|---|------|------|------|
| 5 | --remote-debugging-port | ❌ | EnigmaVB不传参数 |
| 6 | Hook GetCommandLineW | ❌ | 参数注入但端口不开 |
| 7 | CHROMIUM_FLAGS 环境变量 | ❌ | NW.js 0.93不支持(Chrome 80+特性) |
| 8 | renderer --inspect | ❌ | 端口不开 |

### 第三阶段：QPC 运行时分析

| # | 方案 | 结果 | 数据 |
|---|------|------|------|
| 9 | QPC Hook + 栈回溯 | ✅ | 368K调用/30s, 255个唯一nw_elf地址, 3条路径 |

**三条QPC运行时路径：**
- Path 1: `nw+0x2D9532F` (x3,201, V8主循环, 最深frame#54)
- Path 2: `nw+0x28AEE4B`→`nw+0x28AEEE5` (x509/164, 调度器)
- Path 3: `nw+0x2E599DD` (低频, 周期性)

### 第四阶段：nw_elf 内部函数 Hook

| # | 方案 | 目标 | 结果 |
|---|------|------|------|
| 10 | JIT函数入口 | nw+0x2D950E2 | ❌ Total=0 |
| 11 | 调度器入口 | nw+0x28AEDE7 | ❌ 闪退 |

### 第五阶段：V8 API 直接调用

| # | 方案 | 结果 | 卡点 |
|---|------|------|------|
| 12 | RequestInterrupt + HandleScope | ❌ | NFU永远崩溃 |
| 13 | QPC+HandleScope+直调NFU | ❌ | V8重入 |
| 14 | Hook Script::Run | ❌ | Total=0 (NW.js不走公开V8 API) |
| 15 | Hook Script::Compile | ❌ | Total=0 |

### 第六阶段：延迟执行 + Inspector

| # | 方案 | 结果 | 卡点 |
|---|------|------|------|
| 16 | SetTimer 延迟 | ❌ | 零窗口+不处理WM_TIMER |
| 17 | PostMessage | ❌ | 零窗口 |
| 18 | Inspector CDP | ❌ | create()崩溃+connect()重入 |

### 第七阶段：V8 边界回调

| # | 方案 | 结果 | 卡点 |
|---|------|------|------|
| 19 | BeforeCallEntered | ❌ | NFU永远崩溃 |
| 19b | BeforeCallEntered 重试200次 | ❌ | 回调崩溃后V8移除 |
| 20 | MicrotasksCompleted | ⚠️ | 稳定触发27次/30s, Context不可用 |

### 第八阶段：其他AI建议方案

| # | 方案 | AI | 结果 | 卡点 |
|---|------|----|------|------|
| 21 | Context::New Hook | GPT | ❌ | 渲染进程0调用 |
| 22 | 游戏窗口子类化 | 千问 | ❌ | 渲染进程零窗口 |
| 23 | --load-extension | 千问 | ❌ | NW.js 0.93不支持 |
| 24 | libuv/Node | 豆包/GLM | ❌ | 无node.dll |

### 第九阶段：NtReadFile 深入（2026-08-05 GLM 会话）

| # | 方案 | 结果 | 数据 |
|---|------|------|------|
| 25 | evbunpack overlay分析 | ✅ | 4176文件,全部明文,零压缩 |
| 26 | 内存签名扫描 (scan_plugins.c) | ❌ | 393MB扫完 found=0, JS正文非常驻 |
| 27 | NtReadFile v1 | ⚠️ | 拦截到容器PE数据,非JS明文 |
| 28 | NtReadFile v2 (probe_ntread.c) | ❌ | Worker线程DllMain死锁,未完成 |

### 第十阶段：浏览器进程 ReadFile Hook（2026-08-05 主会话 — **突破**）

| # | 方案 | 版本 | 结果 | 关键发现 |
|---|------|------|------|------|
| 29 | 渲染进程 ReadFile hook | v4 (probe_ntread_v4.c) | ⚠️ | 只捕获PE加载, JS文件不在渲染进程加载 |
| 30 | 渲染进程 kernel32 ReadFile | probe_readfile.c | ⚠️ | 同上, JS文件不流经渲染进程ReadFile |
| 31 | **浏览器进程 ReadFile hook** | **v32** | **✅ 突破！** | **MID HIT! 第734次调用截获plugins.js明文** |
| 32 | 浏览器文本扫描器 | v33 | ✅ | **确认文件加载顺序：main.js在readCnt=595** |
| 33 | main.js 主动注入 | **v34** | **✅ 突破！** | **$plugins.push注入成功！游戏弹出QuestLog.js加载错误** |
| 34 | CreateFileW+ReadFile双重服务 | v35 | ⚠️ | CreateFileW检测到但Enigma返回无效句柄 |
| 35 | Enigma模块扫描 | scan_enigma.c | 待测试 | 内存PE模块+API hook扫描器 |
| 36 | **CreateFileW磁盘重定向** | **v36** | **✅ 方案可行！** | **QuestLog文件重定向到磁盘,成功加载！** |

### 第十一阶段：根除残留进程 + 浏览器进程复活路线（2026-08-05）

| # | 方案 | 版本 | 结果 | 关键发现 |
|---|------|------|------|------|
| 37 | DllMain 安全修复 | v36-fixed | ⚠️ | 3条铁律(不CreateThread/不I/O/事件退出),残留仍在 |
| 38 | 根因诊断(bisect) | empty/lazy/hybrid等 | ✅ | **version代理转发**是残留根因。导出return 0 → 无残留 |
| 39 | **v36-final: 无代理+Hook** | **v36-final** | **✅ 最终基底** | 只ReadFile Hook+main.js注入,无version代理,无残留 |
| 40 | **SetTimer复活** | **v38** | **✅ 复活！** | **浏览器进程有消息泵,定时器触发3次/20s** |

### 第十二阶段：浏览器进程 V8 路线（待测试）

| # | 方案 | 目标 | 前次结果(渲染进程) | 浏览器进程预期 |
|---|------|------|-------------------|-------------|
| 41 | Script::Compile/Run Hook | nw.dll+0xA28130/0xA250B0 | Total=0 | 应有调用(JS在此执行) |
| 42 | Context::New Hook | nw.dll+0xA1E9D0 | 0次 | 应有调用(v30确认) |
| 43 | PostMessage | 游戏主窗口 | 零窗口 | 有窗口→可用 |
| 44 | BeforeCallEntered/MCB | nw.dll+0xA47AF0/0xA47F40 | MCB稳定/Context无效 | 配合Context::New可能有效 |

---

## 核心发现: 残留进程根因

**version.dll 代理转发导致进程无法正常退出。** 只要导出函数(GetFileVersionInfo等)有实际的转发逻辑(LoadLibraryExA或GetModuleHandleA,无论ATTACH还是延迟加载),进程关闭后残留。导出返回0 → 无残留。

**根本原因**: 系统version.dll已被Enigma加载(0x727C0000)。我们的version.dll再次引用它(同名模块),Windows loader在卸载时产生模块依赖冲突,进程无法退出。

**最终方案**: version.dll导出全返回0,不做代理转发。NW.js 0.93不需要版本信息。

---

## V8 API 地址表

```
nw_elf.dll base: 0x10000000

v8::Isolate::GetCurrent()               → 0xA17E00
v8::Isolate::GetCurrentContext()        → 0xA17E30
v8::String::NewFromUtf8() MaybeLocal    → 0xA178C0
v8::String::NewFromUtf8() Local         → 0xA3CE60
v8::Script::Compile(src, name)          → 0xA28130
v8::Script::Run()                       → 0xA250B0
v8::Integer::New()                      → 0xA46E20
v8::HandleScope::HandleScope()          → 0xA1E9B0
v8::HandleScope::~HandleScope()         → 0xA1EC60
v8::Context::Global()                   → 0xA3BED0
v8::Isolate::RequestInterrupt()         → 0xA47370
v8::Isolate::AddBeforeCallEnteredCallback() → 0xA47AF0
v8::Isolate::AddMicrotasksCompletedCallback() → 0xA47F40
v8::Context::New()                      → 0xA1E9D0
v8::Context::Enter()                    → 0xA1F8C0
v8::Context::Exit()                     → 0xA1F9B0
v8::Isolate::Enter()                    → 0xA17760

V8Inspector vtable                       → 0x142ADC00
V8Inspector::connect() [vtable+1]        → 0x11081010
V8InspectorClient vtable                 → 0x142A58C0
```

## V8 重入墙实验矩阵（核心数据）

| V8 API | QPC Handler | BeforeCallEntered | MicrotasksCompleted |
|--------|------------|-------------------|-------------------|
| GetCurrent() | ✅ | ✅ 参数传入 | ✅ 参数传入 |
| HandleScope | ✅ | ✅ | ✅ |
| GetContext() | ✅ 0x08xxxxxx | ⚠️ 有时有效 | ❌ 0x0078(无效) |
| Context::Enter | ❌ 崩溃 | ❌ 崩溃 | ❌ 崩溃 |
| Context::Global | — | ❌ 崩溃 | ❌ 崩溃 |
| NewFromUtf8() | ❌ 崩溃 | ⚠️ 返回Iso(不崩) | ⚠️ 返回Iso(不崩) |
| Integer::New(0) | ❌ 崩溃 | ❌ 崩溃 | ❌ 崩溃 |
| connect() | ❌ 崩溃 | ❌ 崩溃 | — |

**MCB (MicrotasksCompleted)**：唯一**稳定触发**(27次/30s)且 API 调用不崩溃的回调，但 Context 不可用(返回无效低地址)

## 两个硬性卡点（已绕过）

1. **V8重入墙**：V8执行栈内任何分配型API（NFU/Compile/Integer::New）必定崩溃
2. **NW.js 0.93架构**：不使用公开V8 API执行JS，走Chromium内部管道

> **突破方式**：不在渲染进程的 V8 层注入，改为在**浏览器进程**的**文件系统层**（kernel32 ReadFile）拦截和修改 JS 文件内容。

## 最终工作架构

```
Game.exe (浏览器进程)
  ├── version.dll (我们的 DLL)
  │     ├── 导出函数 return 0 (不做version代理,避免残留进程)
  │     └── Hook ReadFile → 检测main.js → 注入$plugins.push(QuestLog)
  │
  ├── Enigma VFS fallback: www/js/plugins/QuestLog.js (不存在于VFS)
  │     → 自动fallback到磁盘 www/js/plugins/QuestLog.js
  │
  └── 磁盘文件 (放在 www/js/plugins/ 下,Enigma自动找到):
        QuestLog.js, quests.json, routes.json
```

**关键机制**: 
1. main.js注入让PluginManager去找QuestLog插件
2. Enigma VFS找不到QuestLog.js → 自动fallback到磁盘 www/ 路径
3. 不需要CreateFileW重定向,不需要CreateProcessW Hook
4. version.dll不做代理转发 → 无残留进程
